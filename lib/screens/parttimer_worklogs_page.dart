import 'package:flutter/material.dart';

class PartTimerWorkLogsPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('근무 이력')),
      body: const Center(child: Text('근무 이력 페이지')),
    );
  }
}
